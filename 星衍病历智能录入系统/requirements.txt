# 星衍AI · 智能病历录入系统 — 依赖清单
# 安装: pip install -r requirements.txt

# ─── 核心框架 ───
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=11.0
python-multipart>=0.0.6
slowapi>=0.1.8  # 请求频率限制
pyjwt>=2.0.0    # JWT 认证（auth.py）

# ─── GUI 桌面窗口 ───
pywebview>=4.0
PyQt5>=5.15.0
PyQtWebEngine>=5.15.0

# ─── 语音识别 ───
funasr>=1.0.0
modelscope>=1.9.0  # 模型自动下载
torch>=2.0.0
torchaudio>=2.0.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# ─── NLP / 纠错 ───
jieba>=0.42.1
kenlm>=0.1.0

# ─── 数据处理 ───
pydantic>=2.0.0

# ─── 安全 ───
bcrypt>=4.0.0   # 密码哈希（bcrypt，替代 SHA256）

# ─── 微调语料生成（可选）───
edge-tts>=6.1.0

# ─── 文档导出（可选）───
python-docx>=1.0.0  # Word 导出

# ─── 打包工具（开发时用）───
# pyinstaller>=6.0
