# 2026-08-03 工作日志

## 代码完整性审计（星衍病历智能录入系统）
- **结论：代码完整、可运行。**
- 关键证据：
  - build.spec 声明的 14 个核心业务模块（app_server/asr_engine/corrector/database/template_engine/rule_engine/section_parser/medical_classifier/knowledge_graph/knowledge_qa/voice_command/phrase_library 等）全部存在。
  - 全量 `py_compile` 语法检查通过（退出码 0，0 错误，覆盖根目录全部 .py + tests/）。
  - venv 已装 torch/funasr/fastapi/PyQt5/pywebview/modelscope/jieba/soundfile/uvicorn 等关键依赖；业务代码**不 import kenlm**（requirements 中的 kenlm 为冗余声明，缺失不影响运行）。
  - 数据资源齐全：medical_3gram.pkl(15M)、lm_corpus、术语库、icd10_codes、kg_data 三件套、templates 9套 JSON 均存在且体量正常。
  - 业务代码无未实现桩（TODO/NotImplementedError 仅见于 venv 第三方库）。
- 两个"疑似缺失"项已澄清：
  - `model/` 目录：README 注明为已弃用旧版 Vosk 模型，当前版本不使用（build.spec 历史残留）。
  - `asr_confusion_pairs.json`：运行时从纠错反馈自动生成，加载有 `os.path.exists` 保护，缺失不崩。
- 待办/提醒（非代码完整性，影响运行/发布）：
  - 入口双轨：`main.py`(PyQt5 GUI) 与 `gui_launcher.py`(pywebview+FastAPI) 并存；README 主推 main.py，build.spec 只打 gui_launcher，发布需统一主线。
  - build.spec 仍引用 `model/` 与 `asr_confusion_pairs.json`，打包会告警/失败，建议清理这两项 datas。
  - venv 为 Python 3.14.6，偏离 README 推荐 3.11；当前可运行，生产/分发包建议对齐 3.11。
  - 首次运行仍需联网下载 FunASR 三模型（~2GB），或按 README 离线整包部署。

## 同步完整项目到 iCloud
- 用户要求将完整项目放入 iCloud。已确认 iCloud Drive 路径可用：`~/Library/Mobile Documents/com~apple~CloudDocs`
- 操作：`rsync -a`（排除 `__pycache__`/`.DS_Store`/`*.pyc`）到 `iCloud Drive/星衍病历智能录入系统/`
- 校验：rsync 退出码 0；源=目标文件数 41,108；目标体积 4.5G；顶层文件齐全。
- 范围为用户选定的"完整整个项目"，含 venv(1.6G, 平台特定跨机不可用)、finetune_data/finetune_ckpt(训练产物)、recordings 与 data(患者录音与病历)。
- 风险已提示：① iCloud 免费配额 5GB，4.5G 项目 + 根目录已有照片/视频可能超额，云端上传可能不完整（本地副本已在）；② recordings/data 含患者数据上云端有合规风险，建议生产环境改本地加密备份。
